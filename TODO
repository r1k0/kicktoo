-------------
= Wish list =
-------------

- have a dialog layer that would translate to a working kicktoo import
   have dialogs/ feed from ./kicktoo -u
   ./kicktoo -g talks like ./kicktoo -u -p <profile>
   DO NOT try to interface w/ modules/config.sh, too much work
- make profiles:
    gentoo-luks-lvm.profile
    gentoo-luks-lvm-kbin.profile
    gentoo-noboot-noinitramfs.profile
    gentoo-noboot-noinitramfs-kbin.profile
    gentoo-mdraid-grub2.profile
    gentoo-mdraid-grub2-kbin.profile
- make chroot.sh more usable
- port all working gentoo profiles to funtoo
- find an elegant way to split the kicktoo file for readability and design
   but specific distro conflicting functions into their own distro subfolder
  make a folder for each module, then put the code of each module in different files (ideally one per file)
   and make distro specific subfolders with files same name that overwrite the "default" ones
   i.e. modules/bootloader/bootloader.sh
   i.e. modules/bootloader/bootloader_x86.sh
   i.e. modules/bootloader/funtoo/bootloader.sh <- only if it needs custom code 
   i.e. modules/bootloader/funtoo/bootloader_x86.sh
   i.e. modules/bootloader/exherbo/bootloader.sh <- only if it needs custom code 
   play the sourcing game

--------------------------
= FIXME for next release =
--------------------------

- make variable to modules/default from distfiles.gentoo.org URL (for stage+snapshot)
- fork default gentoo profiles to GPT
- why set timezone cats a file in verbose mode
- double check bootloader stuff
    grub1+autoresume is ok
    look at grub2
  the general consensus seems to be that you need to have at least 1M before the first partition when using MBR 
  or have a dedicated 1M+ grub bios partition under GPT when installing newer grub version with softraid and/or lvm.
  -> do I need to pad 1M free space then for MSDOS partition (non GPT)?
- see if I can adapt lilo too with novikane's commit for grub() and mdraid
